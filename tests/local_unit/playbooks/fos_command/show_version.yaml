---
- hosts: host
  gather_facts: no

  tasks:
  - name: "show version"
    fos_command:
      commands:
        - show version
        - show hardware
  # - name: "get hardware fact"
  #   fos_facts:
  #     gather_subset:
  #      - "config"
