---
- hosts: host
  gather_facts: no

  tasks:
  - name: test match strict
    fos_config:
      lines:
        - lldp transmit
        - lldp receive
        - lldp notification
      parents: interface 0/13
      replace: block