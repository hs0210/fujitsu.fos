---
- hosts: host
  gather_facts: no

  tasks:
  - name: configure interface settings
    fos_config:
      lines:
        # - no shutdown
        - lldp transmit
        - lldp receive
        # - no lldp receive
        - no lldp notification
      parents: interface 0/14
      